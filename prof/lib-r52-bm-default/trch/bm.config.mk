# This profile configures RTPS R52 BM for default feature set.  TRCH BM config
# could be set to whatever by other profiles, so here we enforce the
# dependencies that RTPS R52 BM has on TRCH BM.

CONFIG_RTPS_TRCH_MAILBOX	= 1
