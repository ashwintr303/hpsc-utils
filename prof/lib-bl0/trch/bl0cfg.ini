# BL0 configuration: compiled into a blob and read by BL0 bootloader

[bl1]
# set by profiles that choose and configure BL1
file =
image_address     =
load_address      =
entrypoint_offset =

# Placed into the binary config blob by mkbl0cfg:
# bl1 size, bl1 checksum, and BL0 cfg blob ECC
